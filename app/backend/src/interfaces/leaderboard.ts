/* export interface IUser {
  id: number;
  username: string;
  role: string;
  email: string;
  password: string;
}

export interface IService {
  getAll(data: Omit<IUser, 'id'>): Promise<IUser>;
}

export interface IModel {
  getAll(data: Omit<IUser, 'id'>): Promise<string>;
}
 */
